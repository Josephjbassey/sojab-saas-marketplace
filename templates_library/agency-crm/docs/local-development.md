# Local Development

## Commands
- `make dev`: Starts the application and dependencies.
- `make test`: Runs the test suite using pytest.
- `make migrations`: Generates new database migrations.

## Database
The development setup uses PostgreSQL 15. You can connect to it using any client at `localhost:5432`.
